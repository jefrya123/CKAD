# Fix: template labels match selector
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab10-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
      - name: nginx
        image: nginx
