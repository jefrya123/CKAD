---
phase: 06-content-migration
plan: 05
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - scenarios/domain-1/sc-adapter-pod.yaml
  - scenarios/domain-1/sc-sidecar-volumes.yaml
  - scenarios/domain-2/sc-blue-green.yaml
  - scenarios/domain-3/sc-resource-monitoring.yaml
  - scenarios/domain-4/sc-rbac-clusterrole.yaml
  - scenarios/domain-4/sc-sa-token.yaml
  - scenarios/domain-5/sc-service-nodeport.yaml
  - scenarios/domain-5/sc-dns-custom.yaml
  - archive/study-guide/README.md
autonomous: true
requirements: [CONT-06, CONT-07, CONT-08]

must_haves:
  truths:
    - "Total scenario count is at or above 70"
    - "Each domain has at least 10 scenarios"
    - "Speed drills and cheatsheet are preserved as reference content"
    - "Old markdown scenarios and study guide content are archived"
  artifacts:
    - path: "scenarios/"
      provides: "70+ YAML scenarios across 5 domains"
      contains: "*.yaml"
    - path: "archive/study-guide/"
      provides: "Archived study guide content"
  key_links:
    - from: "scenarios/domain-*/"
      to: "lib/scenario.sh scenario_discover"
      via: "YAML files discovered by glob pattern"
      pattern: "scenarios/domain-*/.*\\.yaml"
---

<objective>
Validate scenario counts, fill gaps to reach 70+ total with 10+ per domain, archive old content, and preserve reference materials.

Purpose: Ensure the scenario library meets the CONT-07 (70+ total) and CONT-08 (10+ per domain) thresholds. Fill any gaps with new scenarios from exercise content. Archive old markdown content.
Output: Complete scenario library meeting all content requirements.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/06-content-migration/06-01-SUMMARY.md
@.planning/phases/06-content-migration/06-02-SUMMARY.md
@.planning/phases/06-content-migration/06-03-SUMMARY.md
@.planning/phases/06-content-migration/06-04-SUMMARY.md

<interfaces>
<!-- Expected scenario counts after Plans 01-04 (approximate):
  Domain 1: 6 sc- + 2 debug- + 4 learn- = ~12 (OK)
  Domain 2: 7 sc- + 2 debug- + 3 learn- = ~12 (OK)
  Domain 3: 6 sc- + 3 debug- + 3 learn- = ~12 (OK)
  Domain 4: 6 sc- + 2 debug- + 3 learn- = ~11 (OK)
  Domain 5: 6 sc- + 3 debug- + 3 learn- = ~12 (OK)
  Total: ~59 scenarios

  Need ~11 more to reach 70. Create from unused exercises.md content.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit scenario counts and create gap-filling scenarios from exercises</name>
  <files>
    scenarios/domain-1/sc-adapter-pod.yaml
    scenarios/domain-1/sc-sidecar-volumes.yaml
    scenarios/domain-2/sc-blue-green.yaml
    scenarios/domain-3/sc-resource-monitoring.yaml
    scenarios/domain-4/sc-rbac-clusterrole.yaml
    scenarios/domain-4/sc-sa-token.yaml
    scenarios/domain-5/sc-service-nodeport.yaml
    scenarios/domain-5/sc-dns-custom.yaml
  </files>
  <action>
    Step 1: Count all YAML scenarios per domain from Plans 01-04 output:
    ```bash
    for d in 1 2 3 4 5; do
      count=$(find scenarios/domain-$d -name "*.yaml" | wc -l)
      echo "Domain $d: $count"
    done
    total=$(find scenarios/domain-* -name "*.yaml" | wc -l)
    echo "Total: $total"
    ```

    Step 2: Identify which domains are below 10 and what the total gap is to reach 70.

    Step 3: Create additional scenarios from the exercise files (domains/*/exercises.md) to fill gaps. Each exercise file has 8-12 exercises; pick the most distinct ones that don't duplicate existing scenarios. Target 2-3 new scenarios per underserved domain.

    Likely gap-fillers from exercises (these are exercises NOT already covered by the 31 markdown scenarios):
    - Domain 1: Adapter container pattern (from exercises.md Ex 3), Sidecar with shared volume (from exercises.md Ex 1 variant)
    - Domain 2: Blue-green deployment pattern (from exercises.md)
    - Domain 3: Resource monitoring with kubectl top (from exercises.md)
    - Domain 4: ClusterRole + ClusterRoleBinding (from exercises.md), ServiceAccount token (from exercises.md)
    - Domain 5: NodePort service type (from exercises.md), Custom DNS configuration (from exercises.md)

    Create each as a standard sc-prefix YAML with validations. These are DRILL scenarios (no learn_intro needed).

    Adjust the number of new scenarios based on the actual audit in Step 1. The goal is >= 70 total with >= 10 per domain.
  </action>
  <verify>
    <automated>total=0; for d in 1 2 3 4 5; do count=$(find scenarios/domain-$d -name "*.yaml" 2>/dev/null | wc -l); echo "Domain $d: $count"; total=$((total+count)); done; echo "Total: $total"; [ "$total" -ge 70 ] && echo "PASS: >= 70 scenarios" || echo "FAIL: < 70 scenarios"</automated>
  </verify>
  <done>Total scenario count >= 70. Each domain has >= 10 scenarios. Gap-filling scenarios created from unused exercise content.</done>
</task>

<task type="auto">
  <name>Task 2: Archive old content and preserve reference materials</name>
  <files>
    archive/study-guide/README.md
  </files>
  <action>
    Step 1: Move old markdown scenarios to archive:
    ```bash
    mkdir -p archive/study-guide/scenarios
    mv scenarios/scenario-*.md archive/study-guide/scenarios/
    mv scenarios/README.md archive/study-guide/scenarios/
    ```

    Step 2: Move old study guide content (tutorials, exercises, quizzes) to archive:
    ```bash
    mkdir -p archive/study-guide/domains
    mv domains/ archive/study-guide/domains/
    mkdir -p archive/study-guide/quizzes
    mv quizzes/ archive/study-guide/quizzes/
    mkdir -p archive/study-guide/troubleshooting
    mv troubleshooting/ archive/study-guide/troubleshooting/
    ```

    Step 3: Preserve speed drills and cheatsheet as reference content (CONT-06). These stay in place:
    - speed-drills/ — keep as-is (aliases.md, one-liners.md, vim-tips.md are reference, not scenarios)
    - cheatsheet.md — keep as-is (kubectl reference)

    Step 4: Create archive/study-guide/README.md explaining what was archived and where to find the YAML scenarios now.

    Step 5: Update the root scenarios/README.md to document the new YAML scenario structure:
    ```markdown
    # Scenarios

    YAML-based scenarios for ckad-drill. Organized by domain.

    ## Domains
    - domain-1/ — Application Design & Build
    - domain-2/ — Application Deployment
    - domain-3/ — Observability & Maintenance
    - domain-4/ — Application Environment, Configuration & Security
    - domain-5/ — Services & Networking

    ## Scenario Types
    - sc-*.yaml — Standard drill/exam scenarios
    - debug-*.yaml — Troubleshooting scenarios (broken resource provided)
    - learn-*.yaml — Learn-mode scenarios (concept text + guided exercise)
    ```

    Do NOT move or delete: speed-drills/, cheatsheet.md, exam-tips/, projects/, practice/, setup/, archive/ (already exists with cka-specific/).
  </action>
  <verify>
    <automated>[ ! -f scenarios/scenario-01-multi-container-pod.md ] && echo "Old scenarios archived" || echo "FAIL: old scenarios still in place"; [ -d archive/study-guide/scenarios ] && echo "Archive exists" || echo "FAIL: no archive"; [ -f speed-drills/aliases.md ] && echo "Speed drills preserved" || echo "FAIL: speed drills missing"; [ -f cheatsheet.md ] && echo "Cheatsheet preserved" || echo "FAIL: cheatsheet missing"</automated>
  </verify>
  <done>Old markdown scenarios moved to archive/study-guide/. Tutorials, exercises, quizzes, and troubleshooting labs archived. Speed drills and cheatsheet preserved in place. scenarios/README.md updated with new structure. archive/study-guide/README.md created.</done>
</task>

</tasks>

<verification>
Total YAML scenario count >= 70. Every domain has >= 10. Old content archived. Reference content preserved.
</verification>

<success_criteria>
- `find scenarios/domain-* -name "*.yaml" | wc -l` returns >= 70
- Each domain-N/ directory has >= 10 YAML files
- No .md scenario files remain in scenarios/ root (all archived)
- speed-drills/ and cheatsheet.md still exist at project root
- archive/study-guide/ contains old markdown content
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-migration/06-05-SUMMARY.md`
</output>
