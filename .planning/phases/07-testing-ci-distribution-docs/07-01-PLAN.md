---
phase: 07-testing-ci-distribution-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/schema.bats
  - test/integration/scenario-lifecycle.bats
  - test/fixtures/valid/all-fields-scenario.yaml
  - test/fixtures/invalid/missing-validations.yaml
  - test/fixtures/invalid/bad-difficulty.yaml
  - test/fixtures/invalid/empty-solution.yaml
  - Makefile
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "`make test-unit` runs all bats unit tests without a cluster and all pass"
    - "`make shellcheck` reports zero warnings on bin/ckad-drill and lib/*.sh"
    - "Schema tests validate known-good and known-bad YAML scenario files"
    - "Integration test file exists with scenario lifecycle tests that can run against a live cluster"
  artifacts:
    - path: "test/unit/schema.bats"
      provides: "YAML schema validation tests for scenario files"
    - path: "test/integration/scenario-lifecycle.bats"
      provides: "End-to-end scenario lifecycle tests against live cluster"
    - path: "test/fixtures/invalid/missing-validations.yaml"
      provides: "Known-bad fixture for schema tests"
    - path: "test/fixtures/invalid/bad-difficulty.yaml"
      provides: "Known-bad fixture with invalid difficulty value"
    - path: "test/fixtures/invalid/empty-solution.yaml"
      provides: "Known-bad fixture with empty solution steps"
  key_links:
    - from: "test/unit/schema.bats"
      to: "test/fixtures/"
      via: "loads valid/invalid YAML files and checks scenario_load behavior"
    - from: "test/integration/scenario-lifecycle.bats"
      to: "bin/ckad-drill"
      via: "runs ckad-drill subcommands against live cluster"
    - from: "Makefile"
      to: "test/"
      via: "make test-unit, make test-integration, make test targets"
---

<objective>
Complete the test suite with schema validation tests for YAML scenarios, integration tests for scenario lifecycle against a live cluster, and verify unit test + shellcheck coverage is comprehensive.

Purpose: Ensure all lib functions are tested without a cluster (TEST-01), integration tests cover full scenario lifecycle (TEST-02), schema tests validate YAML files (TEST-03), and shellcheck passes cleanly (TEST-04).

Output: test/unit/schema.bats, test/integration/scenario-lifecycle.bats, additional test fixtures, updated Makefile
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@test/unit/common.bats (pattern reference for unit test structure)
@test/unit/scenario.bats (existing scenario unit tests)
@test/unit/validator.bats (existing validator unit tests)
@test/fixtures/valid/minimal-scenario.yaml (existing valid fixture)
@test/fixtures/invalid/missing-id.yaml (existing invalid fixture)
@lib/scenario.sh (scenario_load function under test)
@lib/validator.sh (validator functions under test)
@bin/ckad-drill (CLI under integration test)
@Makefile (test targets)

<interfaces>
<!-- Key functions the tests will exercise -->

From lib/scenario.sh:
  scenario_load FILE     — parses YAML, sets SCENARIO_* globals, returns 0/1
  scenario_setup FILE    — creates namespace + applies setup, returns 0/1
  scenario_cleanup       — deletes SCENARIO_NAMESPACE
  scenario_discover DIR  — outputs file paths of all .yaml scenarios
  scenario_filter FILES  — filters by FILTER_DOMAIN/FILTER_DIFFICULTY

From lib/validator.sh:
  validator_run_checks FILE NAMESPACE — runs all validations, returns 0 if all pass

From lib/common.sh:
  EXIT_OK=0, EXIT_ERROR=1, EXIT_NO_CLUSTER=2, EXIT_NO_SESSION=3, EXIT_PARSE_ERROR=4

Existing test fixtures:
  test/fixtures/valid/minimal-scenario.yaml — has id, domain, title, difficulty, time_limit, namespace, description, validations, solution
  test/fixtures/valid/all-checks-scenario.yaml — has all 10 validation check types
  test/fixtures/valid/helm-scenario.yaml — has tags: [helm]
  test/fixtures/invalid/missing-id.yaml — missing id field
  test/fixtures/invalid/missing-domain.yaml — missing domain field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema validation tests and additional invalid fixtures</name>
  <files>test/unit/schema.bats, test/fixtures/invalid/missing-validations.yaml, test/fixtures/invalid/bad-difficulty.yaml, test/fixtures/invalid/empty-solution.yaml, test/fixtures/valid/all-fields-scenario.yaml</files>
  <action>
Create test/unit/schema.bats with bats tests that validate the YAML scenario schema by exercising scenario_load against known-good and known-bad fixtures. Follow the existing test pattern from test/unit/common.bats (setup with CKAD_DRILL_ROOT, load bats helpers, source lib files).

**Schema tests to write (TEST-03):**

1. "scenario_load succeeds on minimal valid scenario" — load test/fixtures/valid/minimal-scenario.yaml, assert success, assert SCENARIO_ID is set
2. "scenario_load succeeds on all-checks scenario" — load test/fixtures/valid/all-checks-scenario.yaml, assert success
3. "scenario_load succeeds on scenario with all optional fields" — load test/fixtures/valid/all-fields-scenario.yaml (create this with learn_intro, hint, tags, setup fields)
4. "scenario_load fails on missing-id fixture" — load test/fixtures/invalid/missing-id.yaml, assert failure or empty SCENARIO_ID
5. "scenario_load fails on missing-domain fixture" — load test/fixtures/invalid/missing-domain.yaml, assert failure or empty SCENARIO_DOMAIN
6. "scenario_load fails on missing-validations fixture" — create test/fixtures/invalid/missing-validations.yaml (valid YAML but no validations field), assert parse behavior
7. "scenario_load handles bad difficulty value" — create test/fixtures/invalid/bad-difficulty.yaml (difficulty: "extreme"), verify scenario_load behavior
8. "scenario_load handles empty solution steps" — create test/fixtures/invalid/empty-solution.yaml (solution.steps is empty array), verify load behavior
9. "valid scenario files in scenarios/ directory all load without error" — iterate over all scenarios/domain-*/*.yaml files using scenario_load, assert each succeeds (smoke test)

**Create test fixture files:**

test/fixtures/valid/all-fields-scenario.yaml:
```yaml
id: all-fields-test
domain: 1
title: "All Fields Test Scenario"
difficulty: easy
time_limit: 120
namespace: test-all-fields
description: "A scenario with every possible field populated."
learn_intro: |
  This is concept text for the learn mode.
hint: "Try using kubectl apply."
tags: []
setup:
  - "kubectl create configmap test-cm --from-literal=key=value -n test-all-fields"
validations:
  - type: resource_exists
    resource: configmap/test-cm
solution:
  steps:
    - "kubectl create configmap test-cm --from-literal=key=value -n test-all-fields"
```

test/fixtures/invalid/missing-validations.yaml:
```yaml
id: missing-validations
domain: 1
title: "Missing Validations"
difficulty: easy
time_limit: 120
namespace: test-missing-val
description: "This scenario has no validations field."
solution:
  steps:
    - "echo done"
```

test/fixtures/invalid/bad-difficulty.yaml:
```yaml
id: bad-difficulty
domain: 1
title: "Bad Difficulty"
difficulty: extreme
time_limit: 120
namespace: test-bad-diff
description: "Invalid difficulty value."
validations: []
solution:
  steps:
    - "echo done"
```

test/fixtures/invalid/empty-solution.yaml:
```yaml
id: empty-solution
domain: 1
title: "Empty Solution"
difficulty: easy
time_limit: 120
namespace: test-empty-sol
description: "Empty solution steps array."
validations: []
solution:
  steps: []
```

In schema.bats setup(), source lib/common.sh and lib/scenario.sh. No cluster required — scenario_load only parses YAML via yq.

Also verify that `make test-unit` and `make shellcheck` both pass as-is (TEST-01, TEST-04 audit). If shellcheck produces any warnings, fix them.
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/cka && bats test/unit/schema.bats</automated>
  </verify>
  <done>Schema tests pass, all valid fixtures load successfully, all invalid fixtures are handled correctly, smoke test of all 70 scenario files passes</done>
</task>

<task type="auto">
  <name>Task 2: Create integration test suite for scenario lifecycle</name>
  <files>test/integration/scenario-lifecycle.bats, Makefile</files>
  <action>
Create test/integration/scenario-lifecycle.bats with bats tests that run the full scenario lifecycle against a live kind cluster (TEST-02). These tests require `ckad-drill start` to have been run first.

**Integration tests to write:**

1. "scenario_setup creates namespace on cluster" — pick a valid scenario fixture (test/fixtures/valid/minimal-scenario.yaml), call scenario_setup, verify namespace exists with `kubectl get namespace`, then call scenario_cleanup
2. "scenario_cleanup removes namespace from cluster" — setup a scenario, then cleanup, verify namespace no longer exists
3. "validator_run_checks passes after solution applied" — load test/fixtures/valid/all-checks-scenario.yaml (or a simple scenario from scenarios/domain-1/), setup, apply solution steps via eval, run validator_run_checks, assert exit 0, cleanup
4. "validator_run_checks fails when solution not applied" — load a scenario, setup, run validator_run_checks WITHOUT applying solution, assert non-zero exit, cleanup
5. "full ckad-drill drill lifecycle" — run `ckad-drill drill --domain 1` (capture output to verify scenario card printed), then `ckad-drill check` (expect failure since nothing applied), then `ckad-drill skip` (verify cleanup)
6. "validate-scenario succeeds on a known-good scenario" — run `ckad-drill validate-scenario scenarios/domain-1/sc-commands-args.yaml` (a simple scenario), assert exit 0

**Test structure:**
- In setup(), source lib files and export CKAD_DRILL_ROOT. Use isolated config dir (CKAD_CONFIG_DIR, CKAD_SESSION_FILE, CKAD_PROGRESS_FILE in temp dirs).
- In teardown(), clean up temp dirs AND any leftover namespaces (safety net).
- Each test must handle its own namespace cleanup in case of failure — wrap scenario_cleanup in teardown or use `trap`.
- Add a guard at top: `if ! kubectl cluster-info &>/dev/null; then skip "No cluster available"; fi`

**Makefile update:**
Verify Makefile has correct targets. The current Makefile already has test-unit, test-integration, and test targets. If `test/integration/` dir doesn't exist, `bats test/integration/` would fail — ensure the directory exists.

The Makefile should also have a `test-schema` convenience target (optional) but at minimum `make test` should run both unit and integration.
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/cka && ls test/integration/scenario-lifecycle.bats && bats test/unit/schema.bats</automated>
  </verify>
  <done>Integration test file exists with 6 lifecycle tests. Tests can be run with `make test-integration` when a cluster is available. `make test` runs both unit and integration suites.</done>
</task>

</tasks>

<verification>
1. `make shellcheck` — zero warnings (TEST-04)
2. `make test-unit` — all unit tests pass including new schema.bats (TEST-01, TEST-03)
3. `ls test/integration/scenario-lifecycle.bats` — integration test file exists (TEST-02)
4. `bats test/unit/schema.bats` — all schema validation tests pass
</verification>

<success_criteria>
- All existing unit tests continue to pass
- Schema tests validate known-good and known-bad YAML fixtures
- Integration test file exists with scenario lifecycle tests
- shellcheck reports zero warnings
- `make test-unit` and `make test` targets work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-testing-ci-distribution-docs/07-01-SUMMARY.md`
</output>
