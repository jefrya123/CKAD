---
phase: 03-cli-drill-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/progress.sh
  - test/unit/progress.bats
  - scenarios/domain-1/sc-multi-container-pod.yaml
  - scenarios/domain-2/sc-configmap-secret.yaml
  - scenarios/domain-3/sc-network-policy.yaml
autonomous: true
requirements:
  - PROG-01
  - PROG-03
  - PROG-04

must_haves:
  truths:
    - "Drill results are recorded to progress.json with scenario_id, passed, time, attempts"
    - "Progress schema is additive-only — missing fields get defaults via jq // default"
    - "Progress file survives upgrades (schema version field, never remove fields)"
    - "Per-domain pass rates are computed correctly from scenario results"
    - "Weak domain recommendation identifies the lowest pass-rate domain"
    - "Streak tracking works: increments on consecutive day, resets on gap"
  artifacts:
    - path: "lib/progress.sh"
      provides: "Progress JSON tracking, stats, streak, recommendation"
      exports: ["progress_init", "progress_record", "progress_read_domain_rates", "progress_read_streak", "progress_read_exam_history", "progress_recommend_weak_domain"]
    - path: "test/unit/progress.bats"
      provides: "Unit tests for progress.sh"
      min_lines: 80
    - path: "scenarios/domain-1/sc-multi-container-pod.yaml"
      provides: "Sample scenario for domain 1 testing"
    - path: "scenarios/domain-2/sc-configmap-secret.yaml"
      provides: "Sample scenario for domain 2 testing"
    - path: "scenarios/domain-3/sc-network-policy.yaml"
      provides: "Sample scenario for domain 3 testing"
  key_links:
    - from: "lib/progress.sh"
      to: "CKAD_PROGRESS_FILE"
      via: "jq read/write to progress.json"
      pattern: "jq.*CKAD_PROGRESS_FILE"
    - from: "lib/progress.sh"
      to: "scenarios/*.yaml"
      via: "domain field in progress maps to scenario domain"
      pattern: "domain"
---

<objective>
Create progress tracking (lib/progress.sh) and 3 sample YAML scenarios for testing.

Purpose: Progress tracking records drill results and computes stats for `ckad-drill status`. Sample scenarios are needed for drill randomization and filter tests in Plan 03. These are independent of session/timer (Plan 01) and can run in parallel.

Output: progress.sh with unit tests, 3 YAML scenario files across different domains.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-drill-mode/03-RESEARCH.md

<interfaces>
<!-- Existing contracts -->

From lib/common.sh:
```bash
CKAD_CONFIG_DIR="${XDG_CONFIG_HOME:-${HOME}/.config}/ckad-drill"
CKAD_PROGRESS_FILE="${CKAD_CONFIG_DIR}/progress.json"
readonly EXIT_OK=0
readonly EXIT_ERROR=1
# Functions: info(), warn(), error(), success()
```

Progress JSON schema (ADR-05):
```json
{
  "version": 1,
  "scenarios": {
    "sc-01-multi-container-pod": {
      "passed": true,
      "time_seconds": 145,
      "attempts": 2,
      "last_attempted": "2026-02-28T10:30:00Z",
      "domain": 1
    }
  },
  "exams": [],
  "streak": { "current": 3, "last_date": "2026-02-28" }
}
```

Scenario YAML format (from test/fixtures/valid/minimal-scenario.yaml):
```yaml
id: test-minimal
domain: 1
title: "Run a minimal pod"
difficulty: easy
time_limit: 60
description: "Create a simple pod."
validations:
  - name: pod_exists
    type: resource_exists
    resource: pod/nginx
solution:
  steps:
    - "kubectl run nginx --image=nginx"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/progress.sh with progress JSON tracking</name>
  <files>lib/progress.sh, test/unit/progress.bats</files>
  <behavior>
    - progress_init creates progress.json with version:1, empty scenarios:{}, empty exams:[], streak:{current:0, last_date:""}
    - progress_init is idempotent — does not overwrite existing file
    - progress_init creates CKAD_CONFIG_DIR if missing
    - progress_record SCENARIO_ID DOMAIN PASSED TIME_SECONDS upserts: sets passed, time_seconds, domain, last_attempted, increments attempts
    - progress_record creates progress.json via progress_init if missing (Pitfall 6)
    - progress_record increments attempts on repeated calls for same scenario
    - progress_read_domain_rates outputs per-domain pass rates (e.g., "1:75 2:50 3:100")
    - progress_read_streak outputs current streak number
    - progress_recommend_weak_domain outputs domain number with lowest pass rate
    - Streak increments on consecutive day, resets on gap, unchanged on same day
    - All jq queries use `// default` for missing fields (ADR-05 additive schema)
  </behavior>
  <action>
    Write tests first in test/unit/progress.bats. Use temp dirs for CKAD_CONFIG_DIR isolation. Test:
    1. progress_init creates valid JSON with version 1
    2. progress_init does not overwrite existing file
    3. progress_record creates file if missing
    4. progress_record stores passed/time/domain/attempts
    5. progress_record increments attempts on second call
    6. progress_read_domain_rates computes correct percentages
    7. progress_read_streak returns current streak
    8. progress_recommend_weak_domain returns lowest domain
    9. Streak: consecutive day increments, gap resets, same day unchanged

    Then implement lib/progress.sh:
    - Header: `# shellcheck shell=bash` + `# shellcheck disable=SC2034`
    - progress_init: mkdir -p, write default JSON via jq -n if file missing
    - progress_record SCENARIO_ID DOMAIN PASSED TIME_SECONDS: atomic update via jq to temp + mv pattern. Compute last_attempted with `date -u +"%Y-%m-%dT%H:%M:%SZ"`. Increment attempts: read existing, default to 0, add 1. Also update streak (see streak logic in research doc). Cross-platform yesterday: try `date -d 'yesterday' +%Y-%m-%d` then fall back to `date -v-1d +%Y-%m-%d`, then fall back to current date minus 86400 seconds via epoch arithmetic.
    - progress_read_domain_rates: jq query grouping scenarios by domain, computing pass rate per domain
    - progress_read_streak: `jq -r '.streak.current // 0'`
    - progress_read_exam_history: `jq -r '.exams // []'`
    - progress_recommend_weak_domain: jq query finding domain with lowest pass rate

    IMPORTANT: Use jq for all operations. Use atomic write (jq > tmp && mv tmp file). Use `// default` everywhere for ADR-05 safety. Use `((n++)) || true` for arithmetic.
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/cka && bats test/unit/progress.bats</automated>
  </verify>
  <done>All progress.bats tests pass. progress_init/record/read functions work correctly with additive schema safety.</done>
</task>

<task type="auto">
  <name>Task 2: Create 3 sample YAML scenarios for drill testing</name>
  <files>scenarios/domain-1/sc-multi-container-pod.yaml, scenarios/domain-2/sc-configmap-secret.yaml, scenarios/domain-3/sc-network-policy.yaml</files>
  <action>
    Create 3 minimal but complete YAML scenario files across different domains and difficulties. These are needed by Plan 03 for drill randomization, filtering, and subcommand tests. They do NOT need to be production-quality — simple pod/configmap/networkpolicy creation scenarios suffice. Full content migration is Phase 6.

    Each scenario MUST have all required fields from SCEN-01: id, domain, title, difficulty, time_limit, description, validations (at least 1 typed check), solution.steps, hint.

    Scenario 1 — scenarios/domain-1/sc-multi-container-pod.yaml:
    - id: sc-multi-container-pod, domain: 1, difficulty: medium, time_limit: 180
    - Task: create a pod with 2 containers (nginx + busybox sidecar)
    - Validations: resource_exists (pod), container_count (2), container_image (nginx)
    - Hint and solution provided

    Scenario 2 — scenarios/domain-2/sc-configmap-secret.yaml:
    - id: sc-configmap-secret, domain: 2, difficulty: easy, time_limit: 120
    - Task: create a ConfigMap and a pod that uses it as env
    - Validations: resource_exists (configmap), container_env check
    - Hint and solution provided

    Scenario 3 — scenarios/domain-3/sc-network-policy.yaml:
    - id: sc-network-policy, domain: 3, difficulty: hard, time_limit: 300
    - Task: create a NetworkPolicy denying all ingress
    - Validations: resource_exists (networkpolicy)
    - Hint and solution provided

    Use realistic namespace names (e.g., "web-team", "config-lab", "secure-ns") per the scenario-defined namespace convention.
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/cka && for f in scenarios/domain-1/sc-multi-container-pod.yaml scenarios/domain-2/sc-configmap-secret.yaml scenarios/domain-3/sc-network-policy.yaml; do echo "--- $f ---" && yq -r '.id' "$f" && yq -r '.domain' "$f" && yq -r '.difficulty' "$f" && yq -r '.validations | length' "$f"; done</automated>
  </verify>
  <done>3 YAML scenario files exist across 3 domains with valid required fields, at least 1 validation each, hint and solution sections present.</done>
</task>

</tasks>

<verification>
```bash
cd /home/jeff/Projects/cka && bats test/unit/progress.bats
shellcheck lib/progress.sh
for f in scenarios/domain-*/sc-*.yaml; do yq -r '.id' "$f"; done
```
All tests pass, shellcheck clean, scenarios parseable.
</verification>

<success_criteria>
- lib/progress.sh exists with progress_init/record/read_domain_rates/read_streak/read_exam_history/recommend_weak_domain
- progress.bats has 9+ tests covering init, record, upsert, domain rates, streak logic, weak domain
- 3 YAML scenario files exist in scenarios/domain-{1,2,3}/ with valid schema
- All tests pass, shellcheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-drill-mode/03-02-SUMMARY.md`
</output>
