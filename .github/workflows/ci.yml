name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint-and-unit:
    name: Shellcheck + Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck
          # Install bats-core
          git clone --depth 1 https://github.com/bats-core/bats-core.git /tmp/bats
          sudo /tmp/bats/install.sh /usr/local
          # Install bats helpers
          git clone --depth 1 https://github.com/bats-core/bats-support.git test/helpers/bats-support
          git clone --depth 1 https://github.com/bats-core/bats-assert.git test/helpers/bats-assert
          # Install yq (needed by scenario.sh)
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.6/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Shellcheck
        run: make shellcheck

      - name: Unit tests
        run: make test-unit

  integration:
    name: Integration Tests (kind cluster)
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: lint-and-unit
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck jq
          # Install bats-core
          git clone --depth 1 https://github.com/bats-core/bats-core.git /tmp/bats
          sudo /tmp/bats/install.sh /usr/local
          # Install bats helpers
          git clone --depth 1 https://github.com/bats-core/bats-support.git test/helpers/bats-support
          git clone --depth 1 https://github.com/bats-core/bats-assert.git test/helpers/bats-assert
          # Install yq
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.6/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          # Install kind
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/v0.25.0/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind

      - name: Create kind cluster
        run: |
          kind create cluster --name ckad-drill --wait 60s

      - name: Shellcheck
        run: make shellcheck

      - name: Unit tests
        run: make test-unit

      - name: Integration tests
        run: make test-integration

      - name: Cleanup
        if: always()
        run: kind delete cluster --name ckad-drill 2>/dev/null || true
